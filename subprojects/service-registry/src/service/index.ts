const registry = (() => {
    const get = () => { return }
    const register = () => { return }
    const unregister = () => { return }
    const clean = () => { return }
    return {
        get,
        register,
        unregister,
        clean
    }
})()

export default registry